


<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:orientation="vertical"
    android:clickable="true"
    android:onClick="toggleFortune"
    tools:context="com.johncuffe.fortunecrunch.app.FortuneCrunch">

    <ImageView
        android:id="@+id/fortune_view"
        android:layout_width="fill_parent"
        android:layout_height="400dp"
        android:layout_centerHorizontal="true"
        android:src="@drawable/fortune_cookie_uncracked"
        />
    <TextView
        android:id="@+id/fortune_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:text="Start text"
        android:textColor="#000000"
        android:textSize="10dp"
        android:maxWidth="120dp"
        android:gravity="center"
        android:layout_alignTop="@+id/fortune_view"
        android:layout_marginTop="153dp"
        android:visibility="gone"
        android:minLines="2"
        />
</RelativeLayout>
